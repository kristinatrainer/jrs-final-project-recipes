module.exports =(app) => {
 
    const recipes = require('../controllers/recipes.controllers')  // this connects to CONTROLLERs file
    
    // app.get('/api/urls', urls.getAllUrls)
    // app.get('/api/urls/:id', urls.getRouteById)

    // app.post('/api/urls', urls.createNewRoute)
    
}